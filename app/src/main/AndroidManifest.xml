<?xml version="1.0" encoding="utf-8"?>

<!-- Authors: Violet Armendariz, Mary Prouty-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.otteramazement.clean_water">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COURSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WelcomeActivity"
            android:label="@string/app_name">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.WELCOMEACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.LOGINACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.REGISTERACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AppActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.APPACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ProfileActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.PROFILEACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WaterReportActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.WATERREPORTACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SourceReportActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.SOURCEREPORTACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SourceReportChoiceActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.SOURCEREPORTCHOICEACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SourceReportListActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.SOURCEREPORTLISTACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WaterAvailabilityActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.WATERAVAILABILITYACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PurityReportChoiceActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.PURITYREPORTCHOICEACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PurityReportActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.PURITYREPORTACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PurityReportListActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.PURITYREPORTLISTEACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HistoricalReportActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.HISTORICALREPORTACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HistoricalReportChoiceActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.HISTORICALREPORTCHOICEACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HistoricalReportGraphActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.HISTORICALREPORTGRAPHACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LocationMapInputActivity"
            android:label="Clean-Water">
            android:theme="@android:style/Full_Background"
            <intent-filter>
                <action android:name="com.example.otteramazement.clean_water.LOCATIONMAPINPUTACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
     The API key for Google Maps-based APIs is defined as a string resource.
     (See the file "res/values/google_maps_api.xml").
     Note that the API key is linked to the encryption key used to sign the APK.
     You need a different API key for each encryption key, including the release key that is used to
     sign the APK for publishing.
     You can define the keys for the debug and release targets in src/debug/ and src/release/.
-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>

    </application>

</manifest>